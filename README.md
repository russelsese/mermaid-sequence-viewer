# mermaid-sequence-viewer
A single-page, client-side tool for creating Mermaid sequence diagrams with live rendering as you type. It autosaves locally, supports light and dark mode, and lets you export diagrams as TXT, SVG, or PNG, including copying PNGs to the clipboard. Runs entirely in the browser with no backend.
